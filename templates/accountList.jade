div#target.col-xs-3
	if models !== "undefined" && models !== null && models.length !== 0 
		for item in models
			a(href='#account/#{item.id}') #{item.get("name")}
			p
	else
		p Seems you have no account yet...
		a(href='#account/new').btn.btn-default Create one